#### JAVA基础

#### JAVA并发

 **1. Java内存模型**
 - 线程之间的共享变量存储在主内存中，每个线程都有一个私有的本地内存，本地内存中存储了该线程以读/写共享变量的副本。
 ![enter description here](https://pic2.zhimg.com/80/v2-3d312429710bd6a11eca171858f67751_720w.jpg)

> [图片引用自](https://zhuanlan.zhihu.com/p/138819184)：https://zhuanlan.zhihu.com/p/138819184

- 这样的模型就带了了内存可见性的问题，多个线程之间存储的副本不同步，解决的方法有两个：**volatile关键字和使用锁**。加锁能解决可见性的原因在于，当一个线程进入 synchronizer 代码块后，线程获取到锁，会清空本地内存，然后从主内存中拷贝共享变量的最新值到本地内存作为副本，执行代码，又将修改后的副本值刷新到主内存中，最后线程释放锁。
  
 **2. volatile关键字**
    *==作用 #673AB7==*：保证变量内存的可见性和禁止指令重排序。
	*==保证可见性 #673AB7==*：当使用volatile修饰变量时，每个线程读取共享变量时会直接从主存中获取，在本地修改后再写回主存，再通过总线嗅探机制通知其他线程该变量的副本已经失效，需要从主存中重新获取。
	*==禁止指令重排序 #673AB7==*：为了提高性能，在遵守 as-if-serial 语义（即不管怎么重排序，单线程下程序的执行结果不能被改变。编译器，runtime 和处理器都必须遵守。）的情况下，编译器和处理器常常会对指令做重排序。指令重排序分为3种：编译器优化重排序、指令级并行重排序、内存系统重排序。
	*==禁止指令重排原理 #673AB7==*：
 **3. synchronized关键字**
 **4. ReentrantLock**
 **5. AQS**
 **6. JUC**
    

#### JVM

#### Spring&SpringBoot

#### Dubbo

#### MySQL

#### Redis

#### MQ

